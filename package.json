{
  "name": "with-typescript",
  "version": "1.0.0",
  "scripts": {
    "dev": "DOTENV_FILE=development next",
    "build": "next build",
    "generate:production": "DOTENV_FILE=production next build && DOTENV_FILE=production next export",
    "generate:staging": "DOTENV_FILE=staging next build && DOTENV_FILE=staging next export",
    "start": "next start",
    "type-check": "tsc"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.21",
    "@types/react": "^16.9.16",
    "@types/react-dom": "^16.9.4",
    "typescript": "4.0"
  },
  "license": "MIT"
}